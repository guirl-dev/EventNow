services:
  postgres:
    image: postgres:latest
    container_name: eventnow-postgres
    environment:
      POSTGRES_DB: eventnow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5431:5432"
    restart: always